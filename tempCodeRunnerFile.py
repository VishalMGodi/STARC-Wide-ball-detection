y:
	# 	if key.char == "w":
	# 		camera_z -= 1 * camera_speed
	# 	elif key.char == 's':
	# 		camera_z += 1 * camera_speed
	# 	elif key.char == 'a':
	# 		camera_x -= 1 * camera_speed
	# 	elif key.char == 'd':
	# 		camera_x += 1 * camera_speed
	# 	elif key.char == 'r':
	# 		camera_x,camera_y,camera_z = CAMERA_X, CAMERA_Y, CAMERA_Z
	# except AttributeError:
	# 	if key == keyboard.Key.space:
	# 		camera_y += 1 * camera_speed
	# 	elif key == keyboard.Key.shift:
	# 		camera_y -= 1 * camera_